<UserControl x:Class="MarvinsAIRARefactored.Controls.MairaAppMenuPopup"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">
    <Grid>

        <Border Background="#000"
                Opacity="0.75"
                MouseDown="Scrim_MouseDown" />

        <Border Background="{DynamicResource Brush.Background.Menu}"
                Margin="20"
                Padding="0,5"
                BorderBrush="{DynamicResource Brush.Border.Subtle}"
                BorderThickness="2"
                CornerRadius="10"
                SnapsToDevicePixels="True"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Rectangle x:Name="Image"
                           Grid.Row="0"
                           HorizontalAlignment="Center"
                           Height="96"
                           Width="342"
                           Margin="20"
                           Fill="{DynamicResource Brush.Foreground.Primary}"
                           RenderOptions.BitmapScalingMode="HighQuality">
                    <Rectangle.OpacityMask>
                        <ImageBrush ImageSource="/MarvinsAIRARefactored;component/Artwork/Misc/app-menu-logo.png"
                                    Stretch="Uniform" />
                    </Rectangle.OpacityMask>
                </Rectangle>

                <Image x:Name="ColorImage"
                       Grid.Row="0"
                       HorizontalAlignment="Center"
                       Height="96"
                       Margin="20"
                       Visibility="Collapsed"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       Stretch="Uniform" />

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding AppMenuItems, ElementName=Root}"
                         SelectedItem="{Binding SelectedAppMenuItem, ElementName=Root, Mode=TwoWay}"
                         BorderThickness="0"
                         Background="Transparent">

                    <ListBox.ItemContainerStyle>

                        <Style TargetType="ListBoxItem">

                            <Setter Property="Visibility"
                                    Value="Visible" />

                            <Setter Property="FocusVisualStyle"
                                    Value="{x:Null}" />

                            <Setter Property="Template">

                                <Setter.Value>

                                    <ControlTemplate TargetType="ListBoxItem">

                                        <Border x:Name="Border"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                BorderThickness="0"
                                                CornerRadius="5"
                                                Margin="5,0">

                                            <ContentPresenter Margin="{TemplateBinding Padding}" />

                                        </Border>

                                        <ControlTemplate.Triggers>

                                            <Trigger Property="IsMouseOver"
                                                     Value="True">
                                                <Setter TargetName="Border"
                                                        Property="Background"
                                                        Value="{DynamicResource Brush.ListItem.Hover}" />
                                            </Trigger>

                                        </ControlTemplate.Triggers>

                                    </ControlTemplate>

                                </Setter.Value>

                            </Setter>

                            <EventSetter Event="PreviewMouseLeftButtonUp" Handler="ListBoxItem_PreviewMouseLeftButtonUp" />

                            <Style.Triggers>

                                <DataTrigger Binding="{Binding IsVisible}"
                                             Value="False">
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>

                            </Style.Triggers>

                        </Style>

                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate>

                        <DataTemplate>

                            <Grid>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="10" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="20" />
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="1"
                                        BorderThickness="2,0,0,0"
                                        Margin="0,10">

                                    <Border.Style>

                                        <Style TargetType="Border">

                                            <Setter Property="BorderBrush"
                                                    Value="Transparent" />

                                            <Style.Triggers>

                                                <DataTrigger  Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                              Value="True">
                                                    <Setter Property="BorderBrush"
                                                            Value="{DynamicResource Brush.ComboItem.Stripe}" />
                                                </DataTrigger>

                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                             Value="True">
                                                    <Setter Property="BorderBrush"
                                                            Value="{DynamicResource Brush.Accent}" />
                                                </DataTrigger>

                                            </Style.Triggers>

                                        </Style>

                                    </Border.Style>

                                </Border>

                                <TextBlock Grid.Column="3"
                                           Style="{StaticResource MairaTextBlockStyle}"
                                           Text="{Binding DisplayName}"
                                           FontSize="30"
                                           Foreground="{DynamicResource Brush.Foreground.Secondary}"
                                           Padding="0,5" />

                            </Grid>

                        </DataTemplate>

                    </ListBox.ItemTemplate>

                </ListBox>

            </Grid>

        </Border>

    </Grid>

</UserControl>
