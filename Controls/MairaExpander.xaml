<UserControl x:Class="MarvinsAIRARefactored.Controls.MairaExpander"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MarvinsAIRARefactored.Controls"
             mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header row: toggle arrow + label text + horizontal rule -->
        <Button x:Name="HeaderButton"
                Grid.Row="0"
                Background="Transparent"
                BorderThickness="0"
                Focusable="False"
                IsTabStop="False"
                Cursor="Hand"
                Margin="0,0,0,8"
                Click="HeaderButton_Click">

            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="OverridesDefaultStyle"
                            Value="True" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Expand/collapse chevron -->
                <TextBlock Grid.Column="0"
                           VerticalAlignment="Center">

                    <TextBlock.Style>
                        <Style TargetType="TextBlock"
                               BasedOn="{StaticResource MairaTextBlockStyle}">
                            <Setter Property="Text"
                                    Value="&#x25BE;" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsExpanded, ElementName=Root}"
                                             Value="False">
                                    <Setter Property="Text"
                                            Value="&#x25B8;" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>

                </TextBlock>

                <!-- Label -->
                <TextBlock Grid.Column="2"
                           Style="{DynamicResource MairaTextBlockStyle}"
                           Text="{Binding Label, ElementName=Root, FallbackValue='Test Label'}"
                           VerticalAlignment="Center" />

            </Grid>

        </Button>

        <!-- Content area — visibility is toggled here; ContentPresenter binds to ExpanderContent DP -->
        <Border x:Name="ContentArea"
                Grid.Row="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility"
                            Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsExpanded, ElementName=Root}"
                                     Value="False">
                            <Setter Property="Visibility"
                                    Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ContentPresenter Content="{Binding ExpanderContent, ElementName=Root}" />
        </Border>

    </Grid>

</UserControl>
